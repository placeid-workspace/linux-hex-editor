# linux-hex-editor
tiny linux hex editor i slapped together in like 5 minutes. dumps a file in hex and lets you overwrite any byte. no ncurses. no gui. no bullshit. just type a file, edit a byte, done.
